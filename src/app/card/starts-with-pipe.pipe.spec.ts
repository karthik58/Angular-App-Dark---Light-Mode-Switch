import { StartsWithPipePipe } from './starts-with-pipe.pipe';

describe('StartsWithPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StartsWithPipePipe();
    expect(pipe).toBeTruthy();
  });
});
